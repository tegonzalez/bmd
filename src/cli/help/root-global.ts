/**
 * Re-export global flags for callers that historically imported `bmdRootGlobalArgs`.
 * Canonical definitions live in {@link ../global-options.ts}.
 */

export { BMD_GLOBAL_ARGS as bmdRootGlobalArgs } from '../global-options.ts';
